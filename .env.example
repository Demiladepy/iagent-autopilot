# Root pointer — runtime loads runtime/.env (see README Quickstart).
# Copy runtime/.env.example → runtime/.env and fill keys there.

# Demo-safe defaults for judges (must match runtime/.env when deploying)
SIMULATOR_MODE=true
DRY_RUN=true
